#include <stdio.h>
#include <stdlib.h>
int main ()
// Desenvolva um programa em C que leia e imprima um número inteiro. Victor
{
    int valor;
    printf("Digite um numero inteiro");
    scanf("%d", &valor);
    printf("O valor digitado ser: %d\n", valor);
    return (0);
}
